# Probe training uses saved activations in the normal repo environment, not capture wrappers.
name: PI0.5 broad 1000 first lifted is target filtered - expert action hidden
question: Is the first unambiguous lifted object the instructed target?
hypothesis_family: object choice / first interaction
intended_claim: first lifted target correctness is linearly decodable from expert action hidden states above metadata baselines
target:
  kind: first_lifted_is_target
  missing_policy: drop
row_filter:
  - column: first_lifted_object
    op: notna
  - column: ambiguous_first_lifted
    op: "=="
    value: false
features:
  module: pi05.expert.layers.*
  tensor_type: hidden_tokens
  token_kind: action
  layers: [0, 4, 8, 12, 17]
  timesteps: all
  policy_calls: [0, 1, 2, 3, 4, 5, 6]
  generation_step: final
  reduction: mean
  dtype: float32
split:
  # Held-out task split is the claim-bearing split; row-level random splits are not enough.
  kind: heldout_task
  column: split
  train_value: train
  selection_value: val_heldout_task
  test_value: test_heldout_task
  eval_values: [val_heldout_task, test_heldout_task]
baseline:
  - majority_class
  - benchmark
  - task_id
  - scene_family
  - task_verb
  - primary_target_object
  - policy_call_index
probe:
  models: [linear]
sweep: [layer, policy_call_index]
